<!-- SPECKIT START -->
For additional context about technologies to be used, project structure,
shell commands, and other important information, read the current plan:
`specs/027-driver-directory/plan.md` (new authenticated page at route `/driver` (singular, coherent with `tour`/`driver/{driver}`; distinct from `/driver/{driver}` manage page) listing every driver with a 3-criterion filter bar — additive, NO migration, NO new dependency, NO existing endpoint touched. ONE new backend endpoint `GET /api/drivers` (route name `drivers.index`, `auth`+`throttle:tour-read`) does the filtering server-side per user directive: thin `DriverDirectoryController::index` → `DriverDirectoryRequest` (`name` nullable string, `modes[]` nullable each `Rule::enum(DeliveryMode)`, `warehouse` nullable `exists`) → `DriverDirectoryService::search` running new `Driver::scopeMatching(name,modes,warehouseId)` (case-insensitive `LOWER(name) LIKE`, per-mode `whereHas` AND, `warehouse_id` eq, eager-load modes+warehouse, `orderBy name`) → `DirectoryDriverData` DTO → `{data:[{id,name,image_url,modes,warehouse_id,warehouse_name}]}` — LIGHT payload, NO workday/road/break figures, NO routing (distinct from frozen `GET /api/tour/drivers`). New web route `GET /driver` (singular) → `DriverPageController::directory` (new method, `manage()` byte-for-byte unchanged) ships `warehouses:{id,name}[]` prop; modes from frontend `DELIVERY_MODES`. Frontend `pages/driver/directory.tsx` + `components/driver/directory-bar.tsx` (name input + mode multi-toggle + warehouse Select, `flex-wrap`) + shared `components/driver/driver-summary.tsx` (avatar+name+mode icons+warehouse block EXTRACTED from `driver-list.tsx`, which now renders `<DriverSummary>`+its figures — behavior-preserving, satisfies Constitution VI no-dup) + `use-drivers-directory` hook (debounced name ~200ms, cancel prior, settle-on-latest — mirrors `use-driver-day`/`use-tour-drivers`) + `DirectoryDriver` type. States: loading spinner / error / exact `no drivers found with current criterias.` on zero rows / rows; row links to `/driver/{id}`. Role-named palette only, no h-overflow 320-2560px. Design artifacts: `research.md`, `data-model.md`, `contracts/drivers-directory.md`, `quickstart.md`). Prior features:
`specs/026-driver-map-bar-fixes/plan.md` (FRONTEND-ONLY presentation fixes over the 025 driver page — 3 files + 1 test, nothing else touched, endpoints/payloads FROZEN. (A) tours now draw on load: `DayLayer` `Layer` drops `beforeId={TOUR_ROUTE_LAYER_ID}` (that layer only existed once a tour was selected → legs never rendered until a click) AND `manage.tsx` removes the selected-tour `<RouteLayer>` (its straight stop-path line overlapped `DayLayer`'s highlighted traced leg = the "two lines at once" bug) + its `TOUR_ROUTE_LAYER_ID` import; `DayLayer` is the single line source, `useDayGeometry` swaps straight→polyline in place; numbered stop markers stay (from `TourMap stops`). (B) `line-opacity` = `isHighlighted ? 1 : (hasSelection ? 0.5 : 0.75)` — neutral no-selection now 0.75 (was 1), selected 1.0 / others 0.5 unchanged; colour/width/dash unchanged. (C) `manage.tsx` moves `<DayBar>` BELOW the map + above the tour list; `day-bar.tsx` makes the weekday a title label above the DATE FIELD ONLY (day-nav group `items-end`: `[prev][weekday-label over date][next]`, arrows on the value row) with all bar labels/values aligned like the tour pages, `flex-wrap` kept. Test edit: `day-layer.test.tsx` no-selection opacity assertion 1→0.75. NO backend, NO dep, NO data-model. Design artifacts: `research.md`, `contracts/ui-map-bar.md`, `quickstart.md`). Prior features:
`specs/025-driver-management-page/plan.md` (new authenticated page `/driver/{driver}` to manage one driver — additive, NO migration, reuse-first. One read `GET /api/driver/{driver}/day?date=` (thin `DriverController::day` → new `DayWorkdayService` + `DayLegsBuilder` + `DriverDayData` DTO) returns identity + ordered day tours (stops+durations) + workday totals + NEUTRAL drawable `WorkdayLeg`s in ONE payload; day totals REUSE `WorkdayEstimator`/`MandatoryBreak` (no candidate, no counterfactual), legs use STORED pivot `start/end` (no recompute on read), connections batched via `TravelTimeService::preload`. Two writes: `PATCH /api/driver/{driver}` multipart (`UpdateDriverRequest`: name req, ≥1 mode, warehouse exists, optional image→`public` disk; client dirty-check enables Update; a WAREHOUSE change triggers a fixed `ConfirmDialog` advisory — no per-date enumeration; existing assignments never rewritten) and `POST /api/driver/{driver}/tour-order` (`TourOrderService` RECOMPUTES each tour's entry/exit for the new sequence via `TourStartSelector` chained + re-measures connections, persists `sequence`+`start/end` in one txn; NORMAL save BLOCKED 422 on any unroutable connection, explicit `force:true` degrades via the selector's lowest-position fallback → mirrors 024; set-mismatch vs current day assignments → 409 refresh; only `sequence`/`start/end` ever change). Frontend `pages/driver/manage.tsx` + `components/driver/*`: identity bar, day bar (prev/next+date+figures+Tour-order Update), `DayLayer`/`DayMarkers` (adapted from `WorkdayLayer`/`WorkdayMarkers`: all-neutral, client-driven selection highlight by tour index, warehouse + `T{n}` markers), sortable `TourList` (NEW dep `@dnd-kit/sortable`, drag handle, hover-secondary/select-primary rows, unfold-to-stops `index/coord/duration`, per-row Edit), `use-driver-day` (fetch+cancel+fallback, mirrors `use-tour-drivers`) + `use-day-geometry` (lazy road trace via frozen `POST /api/tour/geometry` NO `tour_id`, pattern of `use-workday-preview`). Edit round-trip: row Edit → `tour/{tour}/edit` with `return_to` (driver+date); the ONE additive touch to existing code is optional `returnTo` on `TourPageController::edit`+optimize page (absent = byte-for-byte current flow) → auto-return on successful re-optimize, back/cancel returns unsaved, failure stays. REUSED as-is: `TourMap`/`RouteLayer`/`ActionButton`/`ConfirmDialog`/`ModeSelect`/`TourDateInput`/`WorkdayLeg`/`formatDurationHm`, `DriverTourRepository` reads (+new `reorder`/`assignmentsForDay` writes). Endpoints optimize/status/force/geometry/drivers/assign I/O FROZEN (`contracts/frozen-io.md`); design artifacts: `research.md`, `data-model.md`, `contracts/driver-day.md`, `contracts/driver-update.md`, `contracts/tour-order.md`, `contracts/frozen-io.md`, `quickstart.md`). Prior features:
`specs/024-manual-tour-duration/plan.md` (manual tour-duration fallback for when the external routing API is down — additive, no migration. New synchronous `POST /api/tour/force` (thin `TourForceController::force` → `ForceTourRequest extends OptimizeTourRequest` + required `travel_duration_s` 1..86400 → new `TourForcingService::force` → REUSED `TourRecorder`/`TourRepository`) writes a tour with stops in INPUT order (no reorder, no upstream call), manual value → `travel_duration_s` (drive only; per-stop `duration_s` saved unchanged), `total_distance_m` null; response mirrors the optimize `done` payload so the forced tour flows through the existing result + driver-assignment views; edit-in-place via `tour_id` (020 semantics), vanished target → `persist_failed`. Frontend: field + `Force Tour` button in `TourControlBar` shown ONLY on `state.status==='failed'`; hook gains `forceTour(mode,loop,minutes)`; `done` state gains `forced?:boolean`; `ResultSummary` shows a "Manually entered" duration badge. Plus a driver-assignment robustness audit (already best-effort: unknown legs→0+`projected_incomplete`, unroutable→logged straight-line, DB-only assign) — one fix: add a fail-fast `route_connect_timeout` to `OpenStreetRouteClient` (mirrors the TSP client) so a dead host never stalls the pooled `/route` batch. Endpoints optimize/geometry/drivers/assign I/O frozen; design artifacts: `research.md`, `data-model.md`, `contracts/force-tour.md`, `contracts/frozen-io.md`, `quickstart.md`). Prior features:
`specs/023-tour-code-refactor/plan.md` (backend-only, behavior-preserving refactor of the tour-optimization pipeline — SOLID/SRP, short intent-named private methods, verb-methods/noun-vars reading like the business sentence, enforce Controller→Service→Repository/Client roles. New `app/Repositories/TourRepository.php` owns all `Tour`/`Stop` Eloquent access now scattered across `TourRecorder`/`TourPageController`/`OptimizeTourRequest`; decompose `TourOptimizationService::optimize`, `OptimizeTourJob::handle`, `TourRecorder::record`; move `TourPageController::edit` payload-shaping to a service; drop dead `use App\Models\Stop`. Scope widened to the WHOLE route-optimization back-end: also extract `TourAssignmentService` + `DriverAvailabilityService` from their controllers, add `DriverTourRepository` owning the `DB::table('driver_tour')` prior-tour queries + assignment write/sequence, decompose the long service methods (`DriverController::available` ~75L, `TravelTimeService`, `OpenStreet*Client`, etc.). Endpoint I/O + all behavior frozen (`contracts/frozen-io.md`); existing test suite stays green UNCHANGED (only subject-retarget allowed, no new tests); noticed behavior/robustness/perf issues deferred to `observations.md`, not acted on. Out of scope: ALL front-end (shared bar/panel refactor deferred to a separate pass) + vendored starter-kit. Design artifacts: `research.md`, `data-model.md`, `contracts/frozen-io.md`, `observations.md`, `quickstart.md`). Prior features:
`specs/022-mobile-panel-scroll/plan.md` (mobile-only fix so the tour screen's bottom content panel scrolls as one surface — the wrapped-tall orange bar scrolls up and is clipped beneath the fixed map (no overlap, no z-index) so the driver/stop list is reachable, and the bar goes edge-to-edge. CSS-only, additive Tailwind `max-md:` variants (viewport <768px = app's `use-mobile` breakpoint) so desktop classes are literally untouched: panel `max-md:overflow-y-auto max-md:p-0`; `ResultSummary`/`StopList` roots `max-md:h-auto`; `DriverList`/`StopList` `<ul>` `max-md:flex-none`; `TourControlBar`/`ResultSummary` bars `max-md:rounded-none`. Verified by class-presence tests + manual narrow-viewport walkthrough (jsdom evaluates no media queries); design artifacts: `research.md`, `data-model.md`, `contracts/mobile-panel.md`, `quickstart.md`). Prior features:
`specs/021-mobile-responsive/plan.md` (mobile responsiveness for the tour screen — CSS-only. Both top bars (`TourControlBar` root + `ResultSummary` header) `flex-wrap` their controls onto multiple rows on overflow (post-implementation the user chose wrap over scroll for the bars), with `shrink-0` on each bar's child groups so groups wrap as whole units. The reusable Tailwind v4 `@utility scroll-x-contained` (`overflow-x:auto` + `overscroll-behavior-x:contain`) in `app.css` is now applied to each **driver row** so a driver's info scrolls cleanly side-to-side within its border; the `DriverList` `<ul>` gained `min-h-0` (with its existing `flex-1 overflow-y-auto`) so the list scrolls within its box instead of the classic flex-overflow spill. Fits state stays identical; bar menus safe (Radix Select portals to body, date is native input); design artifacts: `research.md`, `data-model.md`, `contracts/responsive-bars.md`, `quickstart.md` — note these still describe the original bar-scroll approach, superseded by wrap). Prior features:
`specs/020-edit-tour/plan.md` (edit an optimized-but-unassigned tour in place — result-view actions become `New · Edit · Assign`; Edit navigates to `tour/{tour}/edit`, served by a new `TourPageController` that hydrates the reused optimize page from an `editTour` prop `{id, mode, loop, stops[{lat,lng,duration_minutes}]}` (no date — unassigned tours carry none); the optimize endpoint gains an optional owned+unassigned `tour_id` threaded `OptimizeTourRequest → TourOptimizationController → TourOptimizationService::optimize → OptimizeTourJob → TourRecorder::record`, which UPDATES the tour + replaces its stops in-transaction instead of creating; no schema change; a vanished queued-edit target surfaces `persist_failed`, never a silent create; design artifacts: `research.md`, `data-model.md`, `contracts/edit-tour.md`, `quickstart.md`). Prior features:
`specs/019-driver-mandatory-breaks/plan.md` (fold legally-mandated rest breaks into the Projected workday — `break = max(workdayBreak[0/30/45min by 6h/9h day], drivingBreak[45min × floor(driving/4h30)])`, driving = day total − all stop seconds; new pure `MandatoryBreak::secondsFor(workdayS, drivingS)` reused for the day with + without the candidate; `WorkdayEstimate` gains `drivingDurationS`, `TourSegment`/`PriorTourLeg` gain `stopSecondsS`; `projected_seconds` now includes `breakWith`, new `added_break` field = breakWith − breakWithout drives a conditional orange "+Required break" figure; counterfactual adds one preloaded `lastPriorEnd→warehouse` connection; design artifacts: `research.md`, `data-model.md`, `contracts/driver-mandatory-breaks.md`, `quickstart.md`). Prior features:
`specs/018-warehouse-origin-markers/plan.md` (when a driver is selected, draw two map point markers — a warehouse `Building2` marker and a "0" marker at the end of the last prior tour, shown only when `previous_tour_end` is non-null — same size as numbered stop markers, `--route-neutral` at 50% opacity; adds additive `warehouse_coordinate` + `previous_tour_end` fields to the drivers row from locals already in the closure, no new routing; new `WorkdayMarkers` component; design artifacts: `research.md`, `data-model.md`, `contracts/warehouse-origin-markers.md`, `quickstart.md`). Prior features:
`specs/017-driver-road-times/plan.md` (driver rows show two grey road-time figures — `time_to_tour` + `time_from_tour`, read from the preloaded connection cache so `projected_seconds` + routing-call count stay unchanged — left of the renamed "Total projected workday" total; only `DriverController`'s row closure changes backend-side;
design artifacts: `research.md`, `data-model.md`, `contracts/driver-road-times.md`, `quickstart.md`). Prior features:
`specs/016-tour-confirm-and-mode/plan.md` (frontend-only: confirm pop-up before "New tour" drops the on-going tour + a delivery-mode selector in the result view that reloads the driver list; shared `ConfirmDialog`, `presentationMode` page state),
`specs/015-projected-path-emphasis/plan.md` (recolor the candidate tour's two bracketing connection legs to primary orange + dim other workday legs to 50%; adds a `highlight` flag to each leg),
`specs/014-driver-workday-preview/plan.md` (map preview of a selected driver's projected workday legs + "Assign Driver" button),
`specs/013-inter-tour-travel/plan.md` (warehouse origin + chained driver workday incl. inter-tour travel & start/end stop selection),
`specs/012-tour-driver-assignment/plan.md` (assign tours to drivers + persist tours/stops),
`specs/011-weekday-label/plan.md` (driver schedule filtering & selected-weekday label),
`specs/008-containerized-deployment/plan.md` (containerized deployment),
`specs/007-stop-duration/plan.md` (per-stop delivery duration & tour duration total),
`specs/006-driver-assignment/plan.md` (delivery driver assignment),
`specs/005-header-theme-menu/plan.md` (header brand & theme menu),
`specs/004-tour-loop-toggle/plan.md` (tour loop toggle),
`specs/003-delivery-mode-selection/plan.md` (delivery mode selection),
`specs/002-road-accurate-route-tracing/plan.md` (road-accurate route tracing) and
`specs/001-delivery-route-optimization/plan.md` (delivery route optimization — complete).
<!-- SPECKIT END -->
