# Copy to `app_key` (no extension) and put the real value on a single line.
# Generate with:  php artisan key:generate --show
# Example shape (DO NOT use this value):
base64:REPLACE_ME_WITH_A_REAL_32_BYTE_BASE64_KEY=
