# Copy to `reverb_app_secret` (no extension) and put the real Reverb app secret on
# a single line. Consumed by `serve`, `queue` and `websocket` (REVERB_APP_SECRET_FILE).
REPLACE_ME_WITH_THE_REVERB_APP_SECRET
