# Copy to `db_password` (no extension) and put the real PostgreSQL password on a
# single line. Used by the `database` service (POSTGRES_PASSWORD_FILE) and by the
# PHP services (DB_PASSWORD_FILE -> DB_PASSWORD).
REPLACE_ME_WITH_A_STRONG_DB_PASSWORD
