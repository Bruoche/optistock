# Copy to `openstreet_api_key` (no extension) and put the real OpenStreet TSP API
# key on a single line. Consumed by `serve` and `queue` (OPENSTREET_API_KEY_FILE).
REPLACE_ME_WITH_THE_OPENSTREET_API_KEY
